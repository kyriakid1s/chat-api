# Server Configuration
PORT=8080
ENVIRONMENT=development
LOG_LEVEL=info

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRY_HOURS=24

# Database Configuration - Option 1: Use individual components
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chatapi
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSLMODE=disable

# Database Configuration - Option 2: Use full connection string (overrides individual components)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/chatapi?sslmode=disable
